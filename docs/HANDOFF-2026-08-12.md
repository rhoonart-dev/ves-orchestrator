# VES 인수인계 — 2026-08-12 (KST 저녁 기준)

새 세션에서 이 문서를 첨부하고 아래 한 줄로 시작하면 된다.

> 이 문서가 VES 운영 인수인계다. 읽고 현재 상태를 DB에서 직접 확인한 뒤,
> "지금 손이 필요한 것" 항목부터 이어서 작업해줘.

이 문서는 **판단의 근거와 함정**만 담는다. 실시간 상태는 항상 DB가 정본이니
문서 숫자를 믿지 말고 §6 쿼리로 다시 재라.

---

## 1. 시스템 지도

3계층이다.

| 계층 | 실체 | 비고 |
|---|---|---|
| 제어 | Supabase `fdidiqdhcyctdbogxkdu` | job_queue·work_orders·sources·review_queue·ops_config |
| 실행 | 맥미니 6대 `mm-01`~`mm-06` | 동질 워커. 잡을 claim 해서 실행 |
| 권리 | Supabase `mehvzxzajydffflqcuuk` (laeebly) | **읽기 전용.** 작품 권리·드라이브 링크 정본 |

- **관제 대시보드**: `https://d3tcpxqaoa3rfi.cloudfront.net`
  S3 `ves-dashboard-039612860860` + CloudFront `E3LW0DDWGND9XX`.
  **깃 푸시로 배포되지 않는다** — §7 참고.
- **코드**: `rhoonart-dev/ves-orchestrator` (맥북 `~/rhoonart/ves-orchestrator`)
- **엔진**: ai-video(`rht-22/ai-video`) · video-localization-project · brain(ai-improvement-edit-video)
  각 맥의 `/opt/ves/engines/<이름>` 에 체크아웃되어 자동 갱신된다.

### 워커 한 바퀴
`heartbeat → diskgc(6h) → updater → claim → executor.run_job`
잡 실행 중엔 루프가 막힌다. lease 갱신 스레드만 돌며 `node_registry.last_seen_at` 을 같이 올린다.

### 하루 흐름 (KST)
`07:00 drive_watch` → `08:00 channels_sync` → `09:00 planner(채널당 1편)` → `10:00 zanmang_daily`
+ 상시: reaper(1분) · drive_balance(5분) · version_watch·reconcile·perf_sync·source_watch(각 60분)

### 채널 20개 (slug)
BGSUNSAK CAREERDAY CINEMAINBED DARAMJI ENDINGSUNSAK HEUNGHAENG IGEOBOGOJA JAEMISHOTS
KIKKIK LOCKERROOM MOLIPDODUK NEOGULBANG REWINDPOCHA SCENELAUNDRY SHORTSSUNSHINE
SHOTNOW TETOCHIP YEOWOON · JP: **SHOTCONE**(ショトコン, 자막만) **LOOPY**(まいにちじゃんまんるぴー, 더빙)

---

## 2. 반드시 지킬 제약 (경위 있는 것들)

- **시크릿·비밀번호·rclone.conf 를 채팅에 붙여넣지 않는다.** AirDrop·직접 입력·드라이브로 옮긴다.
- **DB 마이그레이션은 사용자 확인 후 적용한다.** 적용 후 `applied_migrations` 에 기록한다.
- **laeebly 는 읽기 전용.**
- **공개 전환·지오블락은 사람이 Studio 에서.** 자동 발행은 private·unlisted·예약까지(R9).
  관제에서 '공개' 옵션은 제거돼 있다 — 되살리지 말 것.
- **judge 는 안전 게이트 전용.** 품질 판정으로 승격시키지 않는다.
- **자격증명 입력 금지.** 로그인은 사용자가 한다.
- **터미널 원격 제어 차단은 의도된 가드레일** — 우회하지 않는다.
- **타인(담덕) PR 은 확신 없이 머지하지 않는다.** ai-video 는 다른 사람도 커밋한다.
- 커맨드 블록에 `#` 인라인 주석을 쓰지 않는다(사용자 셸에서 깨진 적 있음).
- 구 시스템(scene_loop)은 **전면 컷오버 완료**. VES 가 20채널 유일 생산자다.

---

## 3. 오늘(8/12) 바뀐 것

### 검수·반려
- **단계별 반려(0021)**: `영상 분석`(새 run) / `스토리 구성`(story 단계부터) / `제작`(render 단계부터).
  반려 사유가 ai-video `--reject-note` 로 넘어가 분석·스토리 프롬프트에 "재작업 지시"로 주입된다.
- **중복 장면 판정 50%**: 겹친 초 ÷ 짧은 쪽 길이 ≥ 0.5 일 때만 같은 장면. 종전 IoU 0.30 + 중심 15초는 폐기.
- 검수 카드에 **제작한 맥**(`🖥 mm-02`) 표기. 지오블락 경고 문구 제거.
- 홈의 "오늘 실패"에서 **이미 해결된 실패는 숨긴다**(같은 대상에 나중 성공이 있으면).

### 소스
- **채널별 소진 분리(0023)**: SNL8 처럼 겹치는 작품이 슬롯을 나눠 굶던 것 해소. 채널마다 자기 use_limit.
- **레거시 이어받기**: `source_usage_legacy` 44행. `_pick_source` 가 work_orders 수에 더해 센다.
  ⚠ 18행은 회차 번호 체계가 달라 실제로는 안 걸린다(`note` 에 "회차체계 상이"·"소스 미등록").
- **오래된 것부터**: 유튜브 등록이 채널 피드(최신순)를 그대로 1화로 매기던 것을 뒤집었다. `max_items` 기본 무제한.
- **길이 규칙**: 등록 시 ffprobe → 10분 미만 1편·10~30분 2편·30분↑ 3편. **3분 이하는 `is_active=false`**.
- **수집 이어받기**: 이미 받은 파일은 rclone `--exclude-from` 으로 제외, 한도 끊김(rc 8·9) 감지로 계속 받음.
  등록 끝난 파일은 캐시에서 즉시 버려 디스크 상한 유지.
- **source_watch(신규, 60분)**: 작품별 `남은 편수 ÷ 배정 채널 수 = 며칠치`. 3일치 이하면 그 작품 폴더만
  겨냥해 즉시 보충 인입. 폴더가 실제로 없으면 `[ALERT]` 로 남기고 잡을 만들지 않는다.
- **drive_balance(신규, 5분)**: 대기 중인 인입 잡을 덜 바쁜 인입 노드로 재배치.

### 신뢰성
- **어피니티 핀은 교체**(추가 아님). 반려 재실행이 노드를 옮기면 `node:*` 가 쌓여 claim 영구 불가였다.
- **localize lease 300초 → 3600초**. 인페인팅이 수십 분인데 5분이라 reaper 가 산 잡을 회수,
  '처음부터 다시'가 무한 반복됐다.
- 인입 **부분 성공 허용**: rclone 디렉토리 modtime 실패로 받아둔 수십 GB 를 통째로 버리던 결함 제거.

### 마이그레이션
0021(단계별 반려) · 0022(source_usage 에 duration_sec) · 0023(채널별 소진 + 레거시) — **모두 적용 완료**.

---

## 4. 지금 손이 필요한 것 (우선순위)

1. **검수** — 대기 15건. 이게 밀리면 다음 날 회전이 멈춘다.
2. **권리사 드라이브에 새 회차 업로드** — 다음 작품들이 바닥이다.
   - 언니네 산지직송(너굴안방): **0편** — 폴더 자체가 감시폴더에 없다
   - 유재석 캠프(이불 속 극장): 레거시 몫까지 세면 소진
   - 놀라운 토요일(숏나우저) 1편 · 김부장(이거보고자) 1편
   외부 감시폴더에 실제로 있는 하위폴더는 `참교육 / 하트시그널 시즌5 / 김부장 / 유재석 캠프` 4개뿐.
3. **mm-06 현지화 자산** — `/opt/ves/engines/video-localization-project` 에 아래가 필요하다.
   - `.env` (459B) — **이것만 있어도 ショトコン 자막·제목·설명이 돈다**(LLM_API_KEY)
   - `outputs/yt_oauth_token.json` (124B) · `outputs/ref_bank/` (7.9MB) · `outputs/GPT-SoVITS/` (3.9GB)
   - `.venv-gsv` — **복사 금지**(shebang 절대경로). mm-06 에서 python3.11 로 재생성.
     잠금 목록: `~/rhoonart/gsv-requirements.lock.txt` (203개)
   - 드라이브: `https://drive.google.com/drive/folders/1wPvdHhFIL7chKxovpxLiO88-bNqzKiXO`
4. **일본어 제목·설명 확인** — 배선은 저장소에 이미 있고 `.env` 키가 없어 못 돌았다.
   첫 JP 발행 전에 실제 출력을 봐야 한다.
5. **업로드 가능일 게이트 자동화** — 아직 사람이 챙긴다. SNL8 은 프로모션 필수·토요일 방영 직후,
   유미의 세포들은 수요일 00시. 권리 리스크 최대 잔여 항목.
6. **관제 소스 창고를 채널별 잔량으로** — 지금은 소스 단위만 보여준다. 뷰 `source_usage_by_channel` 준비됨.

---

## 5. 반복해서 당한 함정 (같은 실수 금지)

- **패치를 만들기 전에 반드시 `git fetch`.** 사용자가 이미 푸시한 것을 모르고 옛 base 로 패치를 만들어
  세 번 충돌시켰다. 항상 `git diff origin/main HEAD` 로 만들고, worktree 로 `git apply --check` 검증.
- **stop-hook 의 "미푸시 N건"은 내 클라우드 클론 기준이라 오보일 수 있다.**
  `git diff origin/main HEAD --stat` 이 비면 이미 올라간 것이다.
- **diff 패치를 채팅에 붙여넣게 하지 말 것.** 빈 줄이 "공백 한 칸"이라 복사 중 사라져 `corrupt patch` 가 난다.
  → `device_commit_files` 로 사용자 맥(`~/rhoonart/ves-fix.patch`)에 직접 써준다.
- **깃 푸시 ≠ 대시보드 배포.** S3 업로드를 따로 해야 한다(§7).
- **`ON CONFLICT DO NOTHING` 은 조용히 거짓 성공을 만든다.** 등록 수를 부풀리고 백필도 막았다.
  지금은 `DO UPDATE` + `RETURNING (xmax=0)` 로 신규/갱신을 구분한다.
- **`CREATE OR REPLACE VIEW` 는 컬럼을 끝에만 붙일 수 있다.** 중간 삽입은 이름 변경으로 취급돼 거부된다.
- **`_pick_source` 는 회차 번호가 '오래된 것 = 낮은 번호'라는 규약에 의존한다.** 등록 쪽을 고칠 때 함께 봐야 한다.
- **`priority` 는 큰 숫자가 먼저다** (`claim` 이 `ORDER BY priority DESC`, 기본 100).
  급한 잡에 50 을 줬다가 오히려 줄 맨 뒤에 세운 적이 있다. 앞세우려면 100 보다 크게.
  잡 하나를 지금 당장 앞세우려면: `UPDATE job_queue SET priority=200, run_after=now() WHERE id='…';`
- **이미 돌고 있는 프로세스는 나중에 넣은 `.env` 를 읽지 않는다.** 파이썬은 시작 시점에 읽는다.
  키·자산을 채운 뒤에는 **잡을 큐로 되돌려 새로 시작**시켜야 한다.
- **`localize` 서브프로세스 타임아웃은 2시간**(`ves/adapters/localize.py`). `max_attempts` 는 3이라
  attempt 2 에서 타임아웃이 나면 곧장 `dead` 다. 오래 걸리는 실행은 attempt 를 미리 0 으로 되돌려 여유를 준다.

---

## 6. 상태 확인 쿼리 (제어 DB)

```sql
-- 한눈에
select (select count(*) from review_queue where status='waiting') as 검수대기,
       (select count(*) from job_queue where status='running')     as 실행중,
       (select count(*) from job_queue where status='pending')     as 대기,
       (select count(*) from job_queue
         where status in ('failed','dead') and updated_at>now()-interval '24 hours') as 실패24h,
       (select left(last_seen_sha,7) from deployments where engine='orchestrator') as 배포sha;

-- 채널별 남은 회차 (0023 이후 정본)
select channel_slug, work_title, min(episode) as 다음회차, count(*) as 남은회차
  from source_usage_by_channel
 where remaining>0 and is_active and (duration_sec is null or duration_sec>180)
 group by 1,2 order by 4, 1;

-- 오늘 체인
select w.channel_slug, w.work_title, w.episode,
       string_agg(j.kind||'='||j.status,' ' order by j.created_at) as chain
  from work_orders w join job_queue j on j.work_order_id=w.id
 where w.created_at > now()-interval '20 hours' group by 1,2,3 order by 1;

-- 워커 상태
select node_id, status, disk_free_gb,
       round(extract(epoch from (now()-last_seen_at))) as 초전
  from node_registry order by node_id;
```

즉시 재계획: `INSERT INTO ops_config(key,value) VALUES('planner_kick', now()::text)
ON CONFLICT(key) DO UPDATE SET value=excluded.value, updated_at=now();`

---

## 7. 배포 절차

### 코드
클라우드 세션에서는 `git push` 가 403 이다(프록시 미인가 — 의도된 가드레일).
그래서 **패치를 사용자 맥에 써주고 사용자가 올린다.**

```
cd ~/rhoonart/ves-orchestrator && git apply ~/rhoonart/ves-fix.patch \
  && git add -A && git commit -m "..." && git pull --rebase && git push
```

올라간 뒤 배포를 당기려면:
`UPDATE deployments SET last_seen_sha='<full sha>', updated_at=now() WHERE engine='orchestrator';`
(안 해도 version_watch 가 한 시간 안에 집는다)

### 대시보드 — 별도다
```
cd ~/rhoonart/ves-orchestrator && aws s3 cp dashboard/index.html \
  s3://ves-dashboard-039612860860/index.html \
  --content-type "text/html; charset=utf-8" --cache-control "no-cache, must-revalidate" \
&& aws cloudfront create-invalidation --distribution-id E3LW0DDWGND9XX --paths "/*"
```
`no-cache` 를 걸어뒀으므로 이후로는 업로드만 하면 즉시 반영된다.

### 검증
`python -m pytest tests/test_pure.py -q` (현재 58개) ·
대시보드는 마지막 `<script>` 블록을 뽑아 `node --check`.
